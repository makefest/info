# Disabling Windows account

- open `Computer Management`
- go to: Computer Management > System Tools > Local Users and Groups > Users
- right-click the user you want to disable and select `Properties`
- on the General tab, make sure `Account is disabled` is checked
- click on `Ok` to confirm

To reenable a disabled Windows account, do the same steps as above but uncheck `Account is disabled`.